(window["webpackJsonpGUI"] = window["webpackJsonpGUI"] || []).push([["addon-entry-project-size-display"],{

/***/ "./node_modules/css-loader/index.js!./src/addons/addons/project-size-display/userstyle.css":
/*!****************************************************************************************!*\
  !*** ./node_modules/css-loader!./src/addons/addons/project-size-display/userstyle.css ***!
  \****************************************************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

exports = module.exports = __webpack_require__(/*! ../../../../node_modules/css-loader/lib/css-base.js */ "./node_modules/css-loader/lib/css-base.js")(false);
// imports


// module
exports.push([module.i, ".sa-project-size-display {\n    position: absolute;\n    bottom: 0;\n    left: 0;\n    right: 0;\n    padding: 6px 10px;\n    padding-right: 70px;\n    background: var(--ui-secondary, #f8f9fa);\n    border-top: 1px solid var(--ui-black-transparent, rgba(0, 0, 0, 0.1));\n    font-size: 11px;\n    display: flex;\n    flex-direction: row;\n    justify-content: space-between;\n    align-items: center;\n    gap: 10px;\n    user-select: none;\n    box-sizing: border-box;\n    height: 32px;\n    z-index: 5;\n}\n\n.sa-project-size-blocks,\n.sa-project-size-project {\n    display: flex;\n    align-items: center;\n    white-space: nowrap;\n    overflow: hidden;\n    text-overflow: ellipsis;\n    color: var(--text-primary, #575e75);\n}\n\n.sa-project-size-blocks {\n    font-weight: 500;\n    flex-shrink: 0;\n}\n\n.sa-project-size-project {\n    color: var(--text-primary, #575e75);\n    flex-shrink: 1;\n    text-align: right;\n}", ""]);

// exports


/***/ }),

/***/ "./src/addons/addons/project-size-display/_runtime_entry.js":
/*!******************************************************************!*\
  !*** ./src/addons/addons/project-size-display/_runtime_entry.js ***!
  \******************************************************************/
/*! exports provided: resources */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "resources", function() { return resources; });
/* harmony import */ var _userscript_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./userscript.js */ "./src/addons/addons/project-size-display/userscript.js");
/* harmony import */ var _css_loader_userstyle_css__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! css-loader!./userstyle.css */ "./node_modules/css-loader/index.js!./src/addons/addons/project-size-display/userstyle.css");
/* harmony import */ var _css_loader_userstyle_css__WEBPACK_IMPORTED_MODULE_1___default = /*#__PURE__*/__webpack_require__.n(_css_loader_userstyle_css__WEBPACK_IMPORTED_MODULE_1__);
/* generated by pull.js */


const resources = {
  "userscript.js": _userscript_js__WEBPACK_IMPORTED_MODULE_0__["default"],
  "userstyle.css": _css_loader_userstyle_css__WEBPACK_IMPORTED_MODULE_1___default.a
};

/***/ }),

/***/ "./src/addons/addons/project-size-display/userscript.js":
/*!**************************************************************!*\
  !*** ./src/addons/addons/project-size-display/userscript.js ***!
  \**************************************************************/
/*! exports provided: default */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony default export */ __webpack_exports__["default"] = (async function (_ref) {
  let addon = _ref.addon,
    console = _ref.console,
    msg = _ref.msg;
  const vm = addon.tab.traps.vm;
  let sizeDisplay;
  let handler = null;
  let isLoading = false;
  function formatFileSize(bytes) {
    if (bytes === 0) return '0 B';
    const k = 1024;
    const sizes = ['B', 'KB', 'MB'];
    const i = Math.floor(Math.log(bytes) / Math.log(k));
    return parseFloat((bytes / Math.pow(k, i)).toFixed(2)) + ' ' + sizes[i];
  }
  function getProjectSize() {
    try {
      const projectJSON = vm.toJSON();
      const jsonString = typeof projectJSON === 'string' ? projectJSON : JSON.stringify(projectJSON);
      const textEncoder = new TextEncoder();
      return textEncoder.encode(jsonString).length;
    } catch (e) {
      console.warn('[Project Size Display] Failed to calculate project size:', e);
      return 0;
    }
  }
  function getBlockCount() {
    let total = 0;
    const targets = vm.runtime.targets || [];
    targets.forEach(target => {
      const sprite = target.sprite || target;
      const blocks = sprite.blocks ? Object.values(sprite.blocks._blocks) : [];
      total += blocks.filter(block => !block.shadow).length;
    });
    return total;
  }
  function getSelectedTargetStorageSize() {
    if (!vm.editingTarget) return '-';
    const target = vm.editingTarget;
    if (target.isStage) return '-';
    const sprite = target.sprite || target;
    let totalSize = 0;
    if (sprite.costumes && Array.isArray(sprite.costumes)) {
      sprite.costumes.forEach(costume => {
        if (costume.asset && costume.asset.data && costume.asset.data.byteLength) {
          totalSize += costume.asset.data.byteLength;
        }
      });
    }
    if (sprite.sounds && Array.isArray(sprite.sounds)) {
      sprite.sounds.forEach(sound => {
        if (sound.asset && sound.asset.data && sound.asset.data.byteLength) {
          totalSize += sound.asset.data.byteLength;
        }
      });
    }
    return totalSize > 0 ? formatFileSize(totalSize) : '-';
  }
  function updateDisplay() {
    if (!sizeDisplay) return;
    try {
      const blockCount = getBlockCount();
      const projectSize = getProjectSize();
      const targetSize = getSelectedTargetStorageSize();
      const blocksText = msg('blocks-count') || 'Blocks';
      const sizeText = msg('project-size') || 'Size';
      sizeDisplay.querySelector('.sa-project-size-blocks').innerText = "".concat(blocksText, ": ").concat(blockCount);
      sizeDisplay.querySelector('.sa-project-size-project').innerText = "".concat(sizeText, ": ").concat(targetSize, "/").concat(formatFileSize(projectSize));
    } catch (e) {
      console.error('[Project Size Display] Error updating display:', e);
      sizeDisplay.innerText = msg('error');
    }
  }
  async function init() {
    while (true) {
      const scrollWrapper = await addon.tab.waitForElement('[class*="sprite-selector_scroll-wrapper_"]', {
        markAsSeen: true,
        reduxEvents: ["scratch-gui/mode/SET_PLAYER", "fontsLoaded/SET_FONTS_LOADED", "scratch-gui/locales/SELECT_LOCALE"],
        reduxCondition: state => !state.scratchGui.mode.isPlayerOnly
      });
      const spriteSelector = scrollWrapper.parentElement;
      const existingDisplay = spriteSelector.querySelector('.sa-project-size-display');
      if (existingDisplay) {
        existingDisplay.remove();
      }
      sizeDisplay = document.createElement('div');
      sizeDisplay.className = 'sa-project-size-display';
      addon.tab.displayNoneWhileDisabled(sizeDisplay);
      sizeDisplay.innerHTML = "\n                <div class=\"sa-project-size-blocks\"></div>\n                <div class=\"sa-project-size-project\"></div>\n            ";
      spriteSelector.appendChild(sizeDisplay);
      updateDisplay();
      let debounce;
      if (handler) {
        vm.off("PROJECT_CHANGED", handler);
        vm.runtime.off("PROJECT_LOADED", handler);
      }
      handler = async () => {
        if (isLoading) return;
        clearTimeout(debounce);
        debounce = setTimeout(updateDisplay, 100);
      };
      vm.on("PROJECT_CHANGED", handler);
      vm.runtime.on("PROJECT_LOADED", () => {
        isLoading = false;
        updateDisplay();
      });
      if (addon.tab.redux) {
        addon.tab.redux.addEventListener("statechanged", e => {
          const actionType = e.detail.action.type;
          if (actionType.startsWith("scratch-gui/project-state/") && (actionType.includes("LOADING") || actionType.includes("START"))) {
            isLoading = true;
          }
        });
      }
      await addon.tab.waitForElement('[class*="sprite-selector_scroll-wrapper_"]', {
        markAsSeen: true,
        reduxEvents: ["scratch-gui/mode/SET_PLAYER"],
        reduxCondition: state => !state.scratchGui.mode.isPlayerOnly
      });
    }
  }
  if (vm.editingTarget) {
    init();
  } else {
    let timeout = setTimeout(function () {
      init();
      clearTimeout(timeout);
    }, 1000);
  }
});

/***/ })

}]);
//# sourceMappingURL=addon-entry-project-size-display.js.map